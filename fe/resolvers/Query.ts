import { Context } from '../types';

export const Query = {
  /*
  user: (_, args, ctx: Context, info) => {
    return ctx.db.query.user(
      {
        where: {
          id: args.id,
        },
      },
      info,
    );
  },*/
};
