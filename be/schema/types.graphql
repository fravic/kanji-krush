interface Node {
  id: ID!
}

type Game {
  subjects: [Subject!]
}

type Subject {
  characters: String!
  meanings: [String!]
  readings: [String!]
}

type AuthPayload {
  user: User!
  token: String!
}

type User implements Node {
  id: ID!
}
